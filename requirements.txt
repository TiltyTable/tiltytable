# Shared Python deps for Jetson / control host.
#
# Install into the project venv:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -r hardware/requirements.txt

pyserial>=3.5
numpy>=1.23
opencv-python-headless>=4.8
flask>=3.0
waitress>=2.1
